# Unpinned so the resolver picks versions with prebuilt wheels for whatever
# Python the host uses (Streamlit Cloud currently builds on Python 3.14, which
# has no wheels for older numpy/matplotlib/pillow -> source builds fail).
streamlit
numpy
scipy
matplotlib
pandas
